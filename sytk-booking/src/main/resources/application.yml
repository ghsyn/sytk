spring:
  application:
    name: sytk-booking
  jackson:
    property-naming-strategy: SNAKE_CASE
  profiles:
    active: dev

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health
