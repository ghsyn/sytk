spring:
  application:
    name: sytk-payment
  jackson:
    property-naming-strategy: SNAKE_CASE
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health
