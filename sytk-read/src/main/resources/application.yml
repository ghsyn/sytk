spring:
  application:
    name: sytk-read
  jackson:
    property-naming-strategy: LOWER_CAMEL_CASE
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: health
